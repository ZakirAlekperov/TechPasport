# Инструкция по интеграции выбора системы координат

## Обзор

Система теперь автоматически определяет местную систему координат (МСК) на основе субъекта РФ, указанного в первой вкладке "Общие сведения".

## Компоненты

### 1. CoordinateSystemRegistry
**Путь:** `src/main/java/zakir/alekperov/domain/locationplan/CoordinateSystemRegistry.java`

Справочник систем координат для всех регионов России. Содержит маппинг:
- **Центральный ФО:** Москва (МСК-77), Московская область (МСК-50), Смоленская область (МСК-67), и др.
- **Северо-Западный ФО:** Санкт-Петербург (МСК-78), Ленинградская область (МСК-47), и др.
- **Южный ФО:** Краснодарский край (МСК-23), Ростовская область (МСК-61), и др.
- **Приволжский ФО:** Татарстан (МСК-16), Башкортостан (МСК-02), и др.
- **Уральский ФО:** Свердловская область (МСК-66), Челябинская область (МСК-74), и др.

**API:**
```java
CoordinateSystem system = CoordinateSystemRegistry.getSystemForRegion("Смоленская область");
// Возвращает: МСК-67 - Местная система координат Смоленской области

String code = system.getCode();  // "МСК-67"
String description = system.getDescription();  // Полное название
```

### 2. BuildingVisualizer
**Обновлен:** Метод `setRegion(String regionName)`

Автоматически определяет систему координат и отображает её код на Canvas:
```java
visualizer.setRegion("Смоленская область");
// Консоль: 🌍 Система координат: МСК-67 для региона Смоленская область
// Canvas внизу: МСК-67: X[435,614.45..435,764.45], Y[21,807,554.45..21,807,654.45]
```

### 3. LocationPlanTabController
**Обновлен:** Добавлен метод `setRegion(String regionName)`

Принимает название региона и передаёт его в visualizer:
```java
locationPlanController.setRegion("Смоленская область");
```

## Интеграция в главное окно

### Шаг 1: Получить ссылки на контроллеры

В главном контроллере приложения (MainWindowController или аналогичном) нужно получить ссылки на оба контроллера:

```java
public class MainWindowController {
    
    @FXML private TabPane mainTabPane;
    
    private CommonInfoTabController commonInfoTabController;
    private LocationPlanTabController locationPlanTabController;
    
    @FXML
    public void initialize() {
        // Предполагается, что вкладки уже загружены
        commonInfoTabController = ... // получить контроллер первой вкладки
        locationPlanTabController = ... // получить контроллер второй вкладки
        
        setupRegionSync();
    }
}
```

### Шаг 2: Настроить синхронизацию региона

```java
private void setupRegionSync() {
    // Получаем TextField "regionField" из CommonInfoTabController
    TextField regionField = commonInfoTabController.getRegionField();
    
    // Слушаем изменения поля "Субъект"
    regionField.textProperty().addListener((observable, oldValue, newValue) -> {
        if (newValue != null && !newValue.isBlank()) {
            // Передаём регион на вторую вкладку
            locationPlanTabController.setRegion(newValue);
            System.out.println("🌍 Регион обновлен: " + newValue);
        }
    });
    
    // Инициализируем, если регион уже заполнен
    if (regionField.getText() != null && !regionField.getText().isBlank()) {
        locationPlanTabController.setRegion(regionField.getText());
    }
}
```

### Шаг 3 (Альтернатива): Через validatedAddress

Если регион берётся из проверенного адреса DaData:

```java
private void setupRegionSync() {
    commonInfoTabController.addAddressValidationListener((validatedAddress) -> {
        if (validatedAddress != null && validatedAddress.getRegion() != null) {
            String region = validatedAddress.getRegion();
            locationPlanTabController.setRegion(region);
            System.out.println("🌍 Регион из ФИАС: " + region);
        }
    });
}
```

## Примеры использования

### Пример 1: Смоленская область

```java
locationPlanController.setRegion("Смоленская область");
// Результат:
// - Система: МСК-67
// - Canvas показывает: "МСК-67: X[435,614.45...], Y[21,807,554.45...]"
```

### Пример 2: Москва

```java
locationPlanController.setRegion("г. Москва");
// Результат:
// - Система: МСК-77
// - Canvas показывает: "МСК-77: X[...], Y[...]"
```

### Пример 3: Неизвестный регион

```java
locationPlanController.setRegion("Неизвестная область");
// Результат:
// - Система: МСК (по умолчанию)
// - Консоль: предупреждение о неизвестном регионе
```

## Поддерживаемые регионы

### Центральный ФО
- г. Москва → МСК-77
- Московская область → МСК-50
- Белгородская область → МСК-31
- Брянская область → МСК-32
- Владимирская область → МСК-33
- Воронежская область → МСК-36
- Ивановская область → МСК-37
- Калужская область → МСК-40
- Костромская область → МСК-44
- Курская область → МСК-46
- Липецкая область → МСК-48
- Орловская область → МСК-57
- Рязанская область → МСК-62
- **Смоленская область → МСК-67** ✅
- Тамбовская область → МСК-68
- Тверская область → МСК-69
- Тульская область → МСК-71
- Ярославская область → МСК-76

### Северо-Западный ФО
- г. Санкт-Петербург → МСК-78
- Ленинградская область → МСК-47
- Новгородская область → МСК-53
- Псковская область → МСК-60
- Калининградская область → МСК-39

### Южный ФО
- Республика Адыгея → МСК-01
- Республика Калмыкия → МСК-08
- Республика Крым → МСК-91
- Краснодарский край → МСК-23
- Астраханская область → МСК-30
- Волгоградская область → МСК-34
- Ростовская область → МСК-61

### Приволжский ФО
- Республика Башкортостан → МСК-02
- Республика Марий Эл → МСК-12
- Республика Мордовия → МСК-13
- Республика Татарстан → МСК-16
- Удмуртская Республика → МСК-18
- Чувашская Республика → МСК-21
- Пермский край → МСК-59
- Кировская область → МСК-43
- Нижегородская область → МСК-52
- Оренбургская область → МСК-56
- Пензенская область → МСК-58
- Самарская область → МСК-63
- Саратовская область → МСК-64
- Ульяновская область → МСК-73

### Уральский ФО
- Курганская область → МСК-45
- Свердловская область → МСК-66
- Тюменская область → МСК-72
- Челябинская область → МСК-74

## Расширение справочника

Для добавления новых регионов редактируйте `CoordinateSystemRegistry.java`:

```java
static {
    // ... существующие регионы
    
    // Добавить новый регион
    SYSTEMS.put("Новая область", 
        new CoordinateSystem("МСК-XX", "Местная система координат Новой области"));
}
```

## Отладка

### Проверка определения системы координат

```java
// Тест в main или unit-тесте
public static void main(String[] args) {
    String[] regions = {
        "Смоленская область",
        "г. Москва",
        "Московская область",
        "Неизвестная область"
    };
    
    for (String region : regions) {
        CoordinateSystemRegistry.CoordinateSystem system = 
            CoordinateSystemRegistry.getSystemForRegion(region);
        
        if (system != null) {
            System.out.println(region + " → " + system.getCode());
        } else {
            System.out.println(region + " → НЕ НАЙДЕНА");
        }
    }
}
```

### Ожидаемый вывод

```
Смоленская область → МСК-67
г. Москва → МСК-77
Московская область → МСК-50
Неизвестная область → НЕ НАЙДЕНА
```

## Примечания

1. **Нечеткое совпадение:** Система поддерживает нечеткий поиск, игнорируя регистр и лишние пробелы.

2. **Автоматическое обновление:** При изменении региона в первой вкладке система координат обновляется автоматически на второй вкладке.

3. **Визуализация:** Код системы координат отображается внизу Canvas вместе с диапазоном реальных координат.

4. **Обратная совместимость:** Если регион не установлен, система использует обобщённое название "МСК".

## Changelog

### 11.02.2026
- ✅ Добавлен справочник систем координат для всех регионов России
- ✅ Обновлен BuildingVisualizer с методом setRegion()
- ✅ Обновлен LocationPlanTabController с методом setRegion()
- ✅ Добавлено отображение кода системы координат на Canvas
- ✅ Создана инструкция по интеграции

---

**Автор:** TechPasport Development Team  
**Дата:** 11 февраля 2026 г.  
**Версия:** 1.0
