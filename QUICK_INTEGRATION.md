# 🚀 Быстрая интеграция системы координат

## Проблема
На Canvas отображается просто **"МСК"** без номера региона.

## Решение
Нужно связать **Первую вкладку** (Общие сведения) со **Второй вкладкой** (Ситуационный план).

---

## 🔧 Инструкция

### Шаг 1: Найдите главный контроллер

Ищите класс, где создаются/загружаются контроллеры вкладок.

Например:
- `MainWindowController.java`
- `TechPasportApplication.java`
- или класс с `@FXML private TabPane`

### Шаг 2: Добавьте код интеграции

```java
public class MainWindowController {
    
    private CommonInfoTabController commonInfoController;
    private LocationPlanTabController locationPlanController;
    
    @FXML
    public void initialize() {
        // 1. Получите контроллеры вкладок (если ещё не получены)
        commonInfoController = ...; // ваш способ получения
        locationPlanController = ...; // ваш способ получения
        
        // 2. Установите listener для автоматической передачи региона
        setupRegionSync();
    }
    
    /**
     * НАСТРОЙКА СИНХРОНИЗАЦИИ РЕГИОНА
     */
    private void setupRegionSync() {
        // Установить listener на изменение региона
        commonInfoController.setRegionChangeListener(region -> {
            locationPlanController.setRegion(region);
        });
        
        // Инициализировать, если регион уже заполнен
        String currentRegion = commonInfoController.getCurrentRegion();
        if (currentRegion != null && !currentRegion.isBlank()) {
            locationPlanController.setRegion(currentRegion);
        }
    }
}
```

---

## ✅ Готово!

После добавления этого кода:

1. При вводе региона в **первой вкладке** ("Субъект")
2. Система автоматически передаст его на **вторую вкладку**
3. На Canvas отобразится **правильный код МСК**!

### Пример результата:
```
Ввод: "Смоленская область"
↓
Canvas: МСК-67: X[435,614.45..435,764.45], Y[21,807,554.45..21,807,654.45] ✅
```

---

## 🔍 Отладка

Если всё ещё показывает "МСК":

### 1. Проверьте консоль
Должны быть сообщения:
```
🌍 Регион изменен: Смоленская область
🌍 Система координат: МСК-67 для региона Смоленская область
```

### 2. Проверьте интеграцию
Добавьте логирование:
```java
private void setupRegionSync() {
    commonInfoController.setRegionChangeListener(region -> {
        System.out.println("✅ [MainWindow] Передаю регион: " + region);
        locationPlanController.setRegion(region);
    });
    
    String currentRegion = commonInfoController.getCurrentRegion();
    System.out.println("🔍 [MainWindow] Текущий регион: " + currentRegion);
    if (currentRegion != null && !currentRegion.isBlank()) {
        locationPlanController.setRegion(currentRegion);
    }
}
```

### 3. Проверьте название региона
Убедитесь, что вводите **точное название**:
- ✅ "Смоленская область"
- ✅ "г. Москва"
- ✅ "Московская область"
- ❌ "Смоленская обл" (неполное)
- ❌ "Смоленск" (нет "область")

💡 **Совет:** Используйте проверку адреса через DaData/ФИАС — она автоматически заполнит регион правильно!

---

## 📚 Полная документация

См. [INTEGRATION_GUIDE.md](INTEGRATION_GUIDE.md) для:
- Полного списка поддерживаемых регионов
- Альтернативных способов интеграции
- Расширения справочника новыми регионами

---

**Автор:** TechPasport Development Team  
**Дата:** 11 февраля 2026 г.  
